{
  "name": "@digital-taco/slang-ipsum",
  "version": "1.0.2",
  "description": "Yeet your ipsum text with the weirdest flex.",
  "main": "./dist/bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup ./src/index.js --file ./dist/bundle.js --format umd --name \"slang-ipsum\"",
    "prepare": "npm run build"
  },
  "files": [
    "dist/"
  ],
  "author": "Zachintosh",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^1.31.0"
  }
}
